# --- Identity -------------------------------------------------------------
# FQDN of this mail server. Must have an A record + matching PTR (rDNS).
MAIL_HOSTNAME=mail.gsit.nl

# Webmail port on the host (http://localhost:8080)
WEBMAIL_PORT=8080
